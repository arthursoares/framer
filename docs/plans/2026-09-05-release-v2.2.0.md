# Release v2.2.0

Arthur explicitly requested a complete release from `develop` to `main`,
including changelog and publication. This authorizes the preparation and
release merges, annotated tag, and GitHub release; no force-push or protection
bypass is needed.

## Plan before changes

1. Use `origin/develop` at `f427a23`, which contains the merged cleanup and UI/UX
   stacks. Previous release: `v2.1.0`; no open PRs remain to include.
2. Prepare v2.2.0 metadata on `release/v2.2.0`: changelog, both app marketing
   versions, CLI version, generated project, README, and release notes.
3. Fix the CLI archive to contain its SwiftPM resource bundle. Verify an
   extracted package with the build-path fallback unavailable, and produce
   checksums. Keep the established unsigned macOS arm64 CLI distribution;
   no valid app-signing identity is installed.
4. Run Core/CLI, macOS, and iOS test gates, a release build, version checks,
   standalone artifact smoke, and independent review.
5. Open and merge preparation into `develop`; open and merge `develop` into
   `main` using reviewed notes and exact-head checks after CI passes.
6. Tag the resulting main commit, watch the release-artifact workflow, inspect
   its archive/checksums, publish the GitHub release, and verify tag/asset/version.
7. Bring the release merge back into develop without changing its source tree.

## Evidence

- All version locations agree on 2.2.0; the Unreleased section is empty.
- Local gates passed: 332 Core/CLI, 78 macOS, and 25 iOS tests, zero failures/skips.
- Optimized arm64 package built and passed version, archive, checksum, and
  relocated GPU/ASCII checks while the build resource bundle was unavailable.
  A binary-only archive reproduced the missing-bundle crash; the corrected
  archive includes `framer_FramerCore.bundle`.
- Release notes are tracked in `docs/release-notes/v2.2.0.md`; `docs/releases/`
  would match the repository's generated-artifact ignore rule.
- Final review and publication evidence will be reported after completion.
